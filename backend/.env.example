# Ejemplo de variables para el backend de Railway + Firebase

# Credenciales de Firebase (puede ser la ruta a un archivo JSON o el JSON completo como texto)
FIREBASE_CREDENTIALS_PATH=./firebase-service-account.json
# o
# FIREBASE_CREDENTIALS_JSON={...}

# Puerto en Railway
PORT=8000
