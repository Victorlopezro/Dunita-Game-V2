# Ejemplo para configuración del juego y del backend remoto

# URL del backend desplegado en Railway
REMOTE_API_URL=http://localhost:8000
REMOTE_USER_ID=default
# Timeout en segundos para llamadas remotas
REMOTE_REQUEST_TIMEOUT=8.0
